<!DOCTYPE html>
<html>
  <head>
    <title>Rails Template</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <link rel="stylesheet" href="/bootstrap.min.css">
  </head>

  <body>
  <nav class="navbar navbar-expand-lg bg-primary" data-bs-theme="dark">
  <div class="container-fluid">
      <a href="/" class="navbar-brand">Home</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
      <% if current_user != nil %>
      <div class="collapse navbar-collapse" id="navbarColor01">
      <ul class="navbar-nav me-auto">
        <li class="nav-item">
        <a href="/users/sign_out" class="nav-link active">Sign out<span class="visually-hidden">(current)</span></a>
        </li>
        <li class="nav-item">
        <a href="/users/edit" class="nav-link active">Edit profile</a>
        </li>

      <% else %>
      <div class="collapse navbar-collapse" id="navbarColor01">
      <ul class="navbar-nav me-auto">
        <li class="nav-item">
        <a href="/users/sign_up" class="nav-link active">Sign up</a>
        </li>
        <li class="nav-item">
        <a href="/users/sign_in" class="nav-link active">Sign in</a>
        </li>
      <% end %>
      </div>
    </nav>
    <%= yield %>
  </body>
</html>
